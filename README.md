# Formulario Simple con React 📝

**React-FormularioSimple-VeraFacundo**

Aplicación web hecha con **React + Vite** que muestra un formulario simple para capturar datos del usuario (por ejemplo nombre, email, mensaje) y luego procesa o muestra esos datos.

---

## 🔍 Descripción

Este proyecto es una práctica de React para trabajar con:

- Formularios controlados (`controlled components`)  
- Manejo del estado (`useState`)  
- Validaciones de datos simples  
- Envío de formulario  
- Mostrar los datos ingresados por el usuario después del envío  

---

## 📦 Requisitos previos

Para correr este proyecto necesitás:

- **Node.js** (versión recomendable: v16 o superior)  
- **npm** o **yarn**

Verificá la instalación con:

```bash
node -v  
npm -v  
# o si usás yarn  
yarn -v  
